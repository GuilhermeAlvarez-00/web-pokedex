import styled from 'styled-components'

export const Container = styled.div`
  width: fit-content;
  padding: 0.5rem 2rem;
  border-radius: 100px;
  border: 1px solid var(--green500);
  box-shadow: 0 0 4px var(--green500);

  p {
    color: var(--green500);
    font-size: 1.2rem;
    text-transform: capitalize;
  }
`
